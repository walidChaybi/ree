import { getLibelle } from "../views/common/widget/Text";
import { MAX_YEAR } from "../views/common/util/DateUtils";

export const MSG_MAX_YEAR = getLibelle(
  `L'année doit être supérieure ou égale à ${MAX_YEAR}`
);
